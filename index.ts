import { cuidades } from "./ciudades";
import { Cliente } from "./cliente";
import { Pais } from "./pais";
import { Poblacion } from "./barrio";
import {
// Ejemplo de uso
  
  // Crear cuidad
  const cuidad = Cine.obtenerInstancia("Mi ciudad");
  
  // Crear barrio
  const los esteros = new barrio("los esteros");
  
  // Agregar cuidad
  cine.agregarcuidad(manta);
  cine.agregarciudad(portoviejo);
  
 